<%= simple_form_for(@project) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name %>
    <%= f.input :desc, as: :text %>
  </div>

  <div class="form-actions">
    <%= f.button :submit, class: 'ghost-btn btn-primary' %>
  </div>
<% end %>
